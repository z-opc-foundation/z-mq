# feature003 发布 `1.3.0` 与抬下游 pin：等用户点头的 2 件

状态：**待点头**。登记时间：09-26 22:5x（下面每条都是那一刻现测；`origin` 那一件已经自己消掉了，见 §2）。

## 1. 等点头的两件

1. **发 Central `1.3.0`**（`deploy -Pcentral`）。
2. **抬 z-boot 的 `z-mq.version` pin**（现在钉 `1.2.0`）。

## 2. 实测现状（命令 + 读数）

```
$ git rev-parse --short origin/main                       → 1d5d4ab
$ git rev-list --count origin/main..HEAD                   → 0        # origin 这条待办已消，不用再点头
$ curl -s -o /dev/null -w '%{http_code}' \
    https://repo1.maven.org/maven2/io/github/yuku123/z-mq-common/<v>/z-mq-common-<v>.pom
      1.2.0 → 200    1.2.1 → 200    1.3.0 → 404
$ grep -n "<version>" pom.xml | head -2                   → :18: 1.2.1（仍是字面量）
$ grep -n "z-mq.version" ../z-boot/pom.xml                → :73: <z-mq.version>1.2.0</z-mq.version>
```

⇒ **对外一格都没传到**：下游经 z-boot BOM 解到的是 `1.2.0`，连 `1.2.1` 都拿不到；W1（真刷盘/恢复/CRC）、W3（oneway/真异步/多订阅/位点）、
W2a–W2d 的接线修复对 `1.2.0` 消费者全部不可见。

⚠ 一条我自己踩过的量具错，留在这里防止再踩：**判"发没发"先读 pom 的 `groupId`**（本仓是 `io.github.yuku123`，见 `pom.xml:16`），
别按 Java 包名 `com.zifang.z.mq` 拼 URL —— 我拼错过一次，三个版本全 404，差点读成"1.2.x 也没发出去"。
且 `curl` 对 404 的 **rc 仍是 0**，判据只有状态码。

## 3. 抬号那笔的两条硬要求（不是新决策，是已裁定的执行细节）

1. **`1.3.0`，不是 `1.2.2`**（用户 09-26 18:3x 让我拍，我拍了 1.3.0，凭据是四条"对外不是 drop-in"：
   `RemotingCommand.encode/decode` 帧字节 short→int、`sendOneway` 超时异常消失、`FLUSH_DISK_TIMEOUT` 含义变更、异步回调换线程）。
   ⇒ **任何会话都不许再发 `1.2.2`**。也别把它夸大成"协议换了"：netty 线上通路本来就是 int，混版集群不炸，炸的只是直接调那对 public API 的人。
2. **抬号必须与 `${revision}` 单一真源同一笔做**：现在根 pom `:18` 还是 `1.2.1` 字面量，卡上量过 **29 处** pom 字面量。
   已验过形状：9 模块全打 `9.9.9-TEST`、`grep 1.2.0` = 0、全套 548/0/0 绿；代价是要配 flatten（flatten 已被别的会话装进 `-Pcentral`）。

## 4. 发布前仍需先闭的（战役内，不点头也会推进）

- `DefaultMQProducer` 那条"`response == null` 就伪造 `FLUSH_DISK_TIMEOUT`"—— 属 W2 射程，先修才能发。
- 排产：`W2e(在飞) → {W2f} → W2h → W2g → W5a → W5b → W5c → W5e → {W5d, W5f 待裁定}`。
  ⇒ 八件广告全部兑现之前是否先对外发一版，仍是要用户拍的一句（我按"先交已闭的"准备，不自行动作）。
